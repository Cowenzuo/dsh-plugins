# 03 文本查看器-测试说明

套件：`ui-cw-textviewer/tests/host.spec.ts`（18 个）+ `tests/renderer-loader.spec.ts`
（2 个，L4 渲染包回归），**共 20 个用例**。直调 `createTextviewerHandler`；
真实临时文件夹具（字节级构造编码场景）。

## 用例逐项

### clampReadLimit（1 个）

- 默认 256KB、钳制到 [1, 1MB]、非法输入回落默认

### encoding detection（3 个）

- BOM 判定：UTF-8 / UTF-16LE / UTF-16BE / 无 BOM
- **严格 UTF-8 失败才回退 GBK**（`中文` 的 GBK 字节夹具）
- UTF-16 走 WHATWG 解码器（BOM 自动剥离）

### isWithin（1 个）

- 平台分隔符精确比较（`D:\work` 不匹配 `D:\workspace`）

### textviewer handler（13 个）

- 未知端点 bad-request
- renderer 端点：正常提供（注入假 bundle）/ 缺失时报错
- 读文件：UTF-8 整读（含中文、行数语义）/ UTF-16LE（BOM 剥离）/
  UTF-16BE / GBK 回退（`中文\r\n` 行数 = 1）
- 首块 NUL 嗅探识别二进制
- 600KB 大文件分块 + 拼接还原（字节偏移语义）
- limit 钳制 + **UTF-16 奇数偏移对齐到偶数**（offset 5 → 4，'ello' 完整）
- 越界 offset 返回空块（truncated=false）
- 越出锁定根拒绝
- 缺失文件 / 目录目标（"not a regular file"）干净失败

## 渲染管线回归测试（L4，已入套件）

`tests/renderer-loader.spec.ts`（2 个，`skipIf` 无 lib/renderer.js 时跳过）：

- **求值 + 渲染**：经真实 `loadRenderer`（含 node:* 桩）求值构建产物 →
  cpp 高亮输出含 `<span class="line">` 与内联色 → MarkdownView 经
  react-dom/server 渲染出 `<h1>` 与 `<table>`（这条路径实际执行 vfile 的
  node:path/process/url 调用，是"无高亮 + 无法读取该文件"事故的回归网）
- **漂移网**：require 超出 shim 的 bundle 必须响亮失败（`vi.resetModules`
  取新实例注入假 bundle）

> 历史事故：渲染包经 vfile 引入 `node:path/process/url`，而 shell 模块表
> 没有 node:* 词、loader 对未知 require 直接 throw → 求值即死 → 全部文件
> 降级纯文本 + 底部"无法读取该文件"。修复 = loader 提供与 vfile 自家
> minpath.browser 等价的最小桩；外层兜底 require 改为硬抛（防漂移）。

## 传输层验证（L3）

- **L3 探针**（改动端点后）：PowerShell 两步——`?token=` 换 cookie →
  POST `/textviewer/<端点>` 信封；验证 read-text/renderer ok、list 已移除
  返回 bad-request（配方见 04 手册）
- **L4 渲染管线**（改动 renderer.tsx 后）：Node 脚本读 `lib/renderer.js`，
  用 `new Function('module','exports','require', ...)` 包装 + require shim
  求值，断言导出三件套可调用、cpp 高亮输出含 `<span class="line">`、
  MarkdownView 渲染出 `<table>`——与浏览器 loader 完全同路径
  （浏览器里 `require` 兜底走 loader 模块表，脚本里用 createRequire 模拟）

## 手工验收清单（每次改动后）

- [ ] 页面加载时查看器**默认隐藏**、零推挤；文件列表点击才出现
- [ ] 标题栏 = 文件名（路径悬停）；隐藏按钮唯一收起入口；再点文件重新打开
- [ ] 底部状态条：编码 · 大小 · 部分标记
- [ ] 渲染：`.cpp` 高亮 + 行号、`.md` GFM 表格、`.yaml` 高亮、`.txt` 纯文本
- [ ] 主题切换 → 高亮明暗跟随
- [ ] 大文件滚动续载：中途滚动**不跳底不抢位置**（几百行处续载后仍在原处），
  精确贴底才连续流式；`sample-big.txt`（1MB）滚到底加载完，`sample-huge.txt`
  （4.3MB）同样完整看全（无上限）；切换文件无卡顿（追加式 DOM / worker 高亮）
- [ ] **拖宽无闪频**（重载按路径键控）；宽度上限 = 视口 − 侧栏 − fileexplorer
  最小宽；视口收缩自动收敛
- [ ] 让位终端：终端展开/收起时查看器底部同步升降
- [ ] 背景色与会话区一致（`--dsw-alias-bg-base`），与 fileexplorer 侧栏色不同
