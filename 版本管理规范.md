# 版本管理与发布规范

> 适用于本工作区三个 dsh 插件（ui-cw-fileexplorer / ui-cw-terminal /
> ui-cw-textviewer）。制定背景：同一会话内版本号连跳 rc2→rc5——每个修复都
> 升一次 rc，把"发布候选序号"当成了"补丁计数器"，显得儿戏。本规范按
> 行业通用语义（SemVer + 持续集成的"发布 ≠ 定版"区分）重定节奏。

## 0. 先分清两个"发布"

本项目日常说的"发布/部署到正式版"，语义上其实有两种，必须分开：

| | 集成发布（integration） | 定版发布（formal release） |
|---|---|---|
| 做什么 | 把当前代码打包集成到正式 profile（3081），供日常真实使用 | 面向里程碑的版本化交付，带验收与记录 |
| 是否构成版本承诺 | 否——只是"正式环境跑最新代码" | 是 |
| 版本号 | **不动** | **唯一允许变更的场合** |
| 谁发起 | 迭代流程内自动发生 | **用户专门发起**（单独活动） |
| 业界类比 | CI/CD 的滚动部署、Maven `-SNAPSHOT` | 正式 release / release candidate |

**两个环境的角色（本项目实况）**：

| 环境 | 角色 |
|---|---|
| 3099（测试 profile） | **验收场**：开发会话内用户逐项点验，视觉确认是关键验收；一切变更先在这里过 |
| 3081（正式 profile） | **使用场**：只接收集成内容供日常使用，不做验收仪式——用户重启 dsh 即用上新内容 |

## 1. 版本号语义（SemVer 2.0.0 子集）

- 形如 `0.1.0` / `0.1.0-rc.5`（主.次.修[-预发布]）
- `-rc.N`：第 N 轮**定版候选**——只在定版流程中使用
- 转正：候选验收通过 → 去标签（`0.1.0`）→ 此后严格 SemVer：
  `0.1.x` 修复、`0.2.x` 新功能、`1.0.0` 破坏性变更/稳定承诺
- **版本号 = 定版基线的标识。与"改了多少代码"无关。**

## 2. 规则

### 2.1 集成发布：不升版本号

```
内部迭代（3099：验收场，永不碰版本号）──► 用户点验通过 ──► publish.ps1
                                                      │
        交付消息：变更 commit 摘要（版本号不变，靠摘要区分内容）
                                                      ▼
                    3081（使用场）：用户重启 dsh 即用上新内容
```

- 以 **package.json 当前版本** 反复重发同名 tgz，版本号不动
- **机制已验证（2026-09 实测）**：同名 tgz 覆盖 vendor → publish.ps1 删除
  `node_modules/@dsh-plugins` → `pnpm install` → pnpm 重算 file: 依赖的
  integrity 并自动更新 lockfile，安装的是**新内容**（无需 `--force`，
  frozen-lockfile 复检无漂移）
- 需要追溯集成批次时用 **git tag**（如 `integration/2026-09-14`）记录，
  不占用版本号

### 2.2 定版发布：唯一升版本号的场合（用户专门发起）

1. 集成内容累积到里程碑，用户拍板定版
2. 定版号 → 升 package.json → 提交（`chore: 版本号升至 x.y.z`）
3. publish → 在 3099 验收场逐项点验
4. 候选期发现缺陷 → 修复 → **同版本重发**（见 2.1 机制）→ 复验，直到通过
5. 验收通过 → 转正（去 `-rc`）或进入下一里程碑 → 记录（tag / 变更日志）

### 2.3 候选内的缺陷修复 ≠ 新版本

- 一次定版候选从交付到验收通过之间，可能有多轮"修复 → 重发 → 复验"
- 这些修复**都不升号**——同一个候选号反复重发，直到验收通过才算该候选结束

## 3. 反模式（对照本次教训）

- ❌ **一个修复升一次 rc**：本次 rc2→rc5 中，md 源码空白、拖动闪烁等修复
  都属于"候选验收中发现的问题"，按本规范应同版本重发；版本号应保持不动，
  直到用户发起定版
- ❌ 把 `rc.N` 当补丁计数（补丁语义属于转正后的 `0.1.x`）
- ❌ 内部迭代期间动版本号（3099 验证阶段，commit hash 即标识）
- ❌ 同版本重发时交付消息不带变更摘要（版本号不变，用户无法区分内容）
- ❌ 定版发布草率（无验收、无记录）——定版是专门活动，不走日常集成流程

## 4. 当前状态

- 三个插件当前 package.json 版本 = 各自的**定版候选**（2026-09 定版）：
  - ui-cw-fileexplorer `0.1.0-rc3`（tag `v0.1-rc3`）
  - ui-cw-terminal `0.1.0-rc2`（tag `v0.1-rc2`）
  - ui-cw-textviewer `0.1.0-rc3`（tag `v0.1-rc3`）
- 曾误升的 textviewer rc3/4/5 已从历史撤销（2026-09：rebase 摘除 chore
  提交，代码零丢失）；其后用户发起定版，textviewer 正式定 `0.1.0-rc3`
- 后续集成（含修复）不再升号；下一轮定版由用户发起

## 5. 关联文档

- 操作细节见 `经验记录/05-发布流程-测试版与正式版.md`
- 提交规范见 `经验记录/12-提交规范与工作流.md`
